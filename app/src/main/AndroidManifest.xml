<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uag.mybusiness">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icone_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icone_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity android:name=".activities.ActivityCadastrarCliente"></activity>
        <activity android:name=".activities.ActivityMenuProduto" />
        <activity android:name=".activities.ActivityListaProdutos" />
        <activity
            android:name=".activities.ActivityCadastrarProduto"
            android:label="Cadastro de Produto" />
        <activity
            android:name=".activities.ActivityMenu"
            android:label="Menu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>